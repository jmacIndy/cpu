Some key GIT commands:

Install git
   sudo apt-get install git

Setup git
   *** these should match user name and email in github
   git config --global user.name "<user name>"
   git config --global user.email "<user email>"

git init - start using git in a project

create README.md

get add <filename>
or
get add .

git commit -m "<commit message>"

create repository on github with same name

git remote add origin https://github.com/<user name>/<project>.git

git push origin master

